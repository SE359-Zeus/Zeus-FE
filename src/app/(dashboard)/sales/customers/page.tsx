import { CustomersView } from '@/features/sales/customers/components/CustomersView'

export const metadata = {
  title: 'Customers | MRP Orchestrator',
}

export default function CustomersPage() {
  return <CustomersView />
}